services:
  # this is nginx example
  nginx-example:
    image: nginx:latest
    container_name: nginx-example
  
  # this is golang example
  golang-example:
    image: golang:alpine3.23
    container_name: golang-example